server:
  port: 8081
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HernanRestrepo95/ms-course-youtube.git
          searchPaths: config-data
          username: HernanRestrepo95
          password: Recreo1995
  security:
    user:
      name: root
      password: s3cr3t